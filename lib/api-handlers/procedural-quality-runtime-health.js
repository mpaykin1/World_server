const crypto=require('crypto');let createClient=null;try{({createClient}=require('@supabase/supabase-js'))}catch(_){}
function db(){const k=process.env.SUPABASE_SECRET_KEY||process.env.SUPABASE_SERVICE_ROLE_KEY;return createClient&&process.env.SUPABASE_URL&&k?createClient(process.env.SUPABASE_URL,k,{auth:{persistSession:false}}):null}
const clean=x=>JSON.parse(JSON.stringify(x||{}).slice(0,30000)),num=v=>Number.isFinite(Number(v))?Number(v):null;
module.exports=async function(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='POST')return res.status(405).json({ok:false});const b=clean(req.body),row={id:crypto.randomUUID(),schema_version:10,app_path:String(b.appPath||'/').slice(0,240),temporal_score:num(b.temporalScore),p95_frame_ms:num(b.p95FrameMs),jank_rate:num(b.jankRate),possible_leak:!!b.possibleLeak,thermal_tier:num(b.thermalTier),shader_prewarm_failed:Number(b.shaderPrewarmFailed||0),metrics:b.metrics||{},created_at:new Date().toISOString()};const d=db();if(!d)return res.status(200).json({ok:true,version:10,persistent:false,id:row.id});try{const o=await d.from('procedural_quality_runtime_health').insert(row);if(o.error)throw o.error;return res.status(200).json({ok:true,version:10,persistent:true,id:row.id})}catch(e){return res.status(200).json({ok:true,version:10,persistent:false,id:row.id,warning:String(e?.message||e).slice(0,180)})}};
