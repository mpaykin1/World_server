const crypto=require('crypto');let createClient=null;try{({createClient}=require('@supabase/supabase-js'))}catch(_){}
function db(){const k=process.env.SUPABASE_SECRET_KEY||process.env.SUPABASE_SERVICE_ROLE_KEY;return createClient&&process.env.SUPABASE_URL&&k?createClient(process.env.SUPABASE_URL,k,{auth:{persistSession:false}}):null}
const clean=x=>JSON.parse(JSON.stringify(x||{}).slice(0,30000));
module.exports=async function(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='POST')return res.status(405).json({ok:false});const b=clean(req.body),row={id:crypto.randomUUID(),schema_version:9,cycle:Number(b.cycle||0),status:String(b.status||'unknown').slice(0,32),issues:b.issues||[],repairs:b.repairs||[],checks:b.checks||{},created_at:new Date().toISOString()};const d=db();if(!d)return res.status(200).json({ok:true,persistent:false,id:row.id});try{const o=await d.from('procedural_quality_repair_cycles').insert(row);if(o.error)throw o.error;return res.status(200).json({ok:true,persistent:true,id:row.id})}catch(e){return res.status(200).json({ok:true,persistent:false,id:row.id,warning:String(e?.message||e).slice(0,180)})}};
