#!/usr/bin/env node
'use strict';const fs=require('fs');const path=require('path');const f=path.resolve(process.cwd(),'data/app-release-registry.json');if(!fs.existsSync(f))throw new Error('catalog registry missing');const j=JSON.parse(fs.readFileSync(f,'utf8'));j.apps||={};const base={title:'Pixel Panorama 360',kind:'tool',goldenStandard:'v2',description:'Animated pixel-art 360 panorama factory and viewer',required:['mouse-look','touch-look','pinch-zoom','gyro','pixel-crisp','multires-streaming','worker-job','panorama-manifest']};j.apps['pixel-panorama-360']=process.argv.includes('--certify')?{...base,visible:true,status:'certified'}:{...base,visible:false,status:'candidate'};fs.writeFileSync(f,JSON.stringify(j,null,2)+'\n');console.log(JSON.stringify({ok:true,app:j.apps['pixel-panorama-360']},null,2));
