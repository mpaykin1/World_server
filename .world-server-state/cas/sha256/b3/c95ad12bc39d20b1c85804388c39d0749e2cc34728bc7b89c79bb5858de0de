'use strict';
(function(root,factory){if(typeof module==='object'&&module.exports)module.exports=factory();else root.WorldMeshletExperiment=factory()})(typeof globalThis!=='undefined'?globalThis:this,function(){
 function partition(indices,opt={}){const mv=opt.maxVertices||64,mt=opt.maxTriangles||126;if(!indices||indices.length%3)throw new Error('triangle indices required');const out=[];let cur={vertices:[],triangles:[]},map=new Map();const flush=()=>{if(cur.triangles.length)out.push(cur);cur={vertices:[],triangles:[]};map=new Map()};for(let i=0;i<indices.length;i+=3){const tri=[+indices[i],+indices[i+1],+indices[i+2]],needed=tri.filter(v=>!map.has(v)).length;if(cur.triangles.length/3>=mt||cur.vertices.length+needed>mv)flush();for(const v of tri)if(!map.has(v)){map.set(v,cur.vertices.length);cur.vertices.push(v)}cur.triangles.push(...tri.map(v=>map.get(v)))}flush();return out}
 async function probe(){if(typeof navigator==='undefined'||!navigator.gpu)return{status:'FALLBACK_ONLY',webgpu:false};try{const a=await navigator.gpu.requestAdapter();return{status:a?'EXPERIMENT_READY':'FALLBACK_ONLY',webgpu:!!a}}catch(e){return{status:'FALLBACK_ONLY',webgpu:false,error:String(e?.message||e)}}}
 return{version:'5.0.0',partition,probe,limits:{maxVertices:64,maxTriangles:126},policy:'Experimental only; WebGL stays mandatory fallback until real device evidence wins.'}
});
