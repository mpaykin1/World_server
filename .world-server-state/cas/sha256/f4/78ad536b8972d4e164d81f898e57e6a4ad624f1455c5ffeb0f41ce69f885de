#!/usr/bin/env python3
"""Canonical V8 production preparer.

Heavy source-locked GI/reflection work defaults to the content-addressed distributed bake farm;
cheap lossless metadata and runtime quality flags are prepared immediately. Use --heavy-bakes local
only on a workstation/bake node when synchronous baking is intentionally desired.
"""
from prepare_world_v6 import main
if __name__=='__main__':
    raise SystemExit(main())
