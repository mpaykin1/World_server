'use strict';
function vec(v={}){return{x:Number(v.x||0),y:Number(v.y||0),z:Number(v.z||0)}}function sub(a,b){a=vec(a);b=vec(b);return{x:a.x-b.x,y:a.y-b.y,z:a.z-b.z}}function add(a,b){a=vec(a);b=vec(b);return{x:a.x+b.x,y:a.y+b.y,z:a.z+b.z}}function mul(a,s){a=vec(a);return{x:a.x*s,y:a.y*s,z:a.z*s}}function len(a){a=vec(a);return Math.hypot(a.x,a.y,a.z)}function norm(a,fallback={x:0,y:0,z:1}){a=vec(a);const l=len(a);return l>1e-8?mul(a,1/l):vec(fallback)}function dot(a,b){a=norm(a);b=norm(b);return a.x*b.x+a.y*b.y+a.z*b.z}
function feetForwardFromTravel(velocity,last={x:0,y:0,z:1}){const v=vec(velocity);v.y=0;return len(v)>1e-5?norm(v,last):norm(last)}function attackDirectionFromFeet(feetForward){return norm({...vec(feetForward),y:0})}function shieldPoint(torso,enemy,clearance=.45){const d=norm(sub(enemy,torso));return add(torso,mul(d,Math.max(0,Number(clearance)||0)))}function isBetween(point,a,b,tolerance=.08){const ab=sub(b,a),ap=sub(point,a),den=Math.max(1e-9,len(ab)**2),t=(ap.x*ab.x+ap.y*ab.y+ap.z*ab.z)/den;const proj=add(a,mul(ab,t));return t>=0&&t<=1&&len(sub(point,proj))<=tolerance}function validateGrip(kind,grips=[]){const k=String(kind||'').toLowerCase(),n=Array.isArray(grips)?grips.filter(Boolean).length:0;if(k==='pistol')return n===1;if(k==='rifle'||k==='machinegun'||k==='machine-gun'||k==='automatic-rifle')return n>=2;return true}
module.exports={vec,sub,add,mul,len,norm,dot,feetForwardFromTravel,attackDirectionFromFeet,shieldPoint,isBetween,validateGrip};
