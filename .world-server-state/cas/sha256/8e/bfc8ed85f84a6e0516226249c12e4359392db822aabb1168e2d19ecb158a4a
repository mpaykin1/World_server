# v11 CPU-FIRST
- Server GPU removed from mandatory readiness/promotion path.
- Added adaptive CPU scheduler with load/RAM backpressure and reserved core.
- Added real Node worker_threads CPU pool and parallel repository scan.
- Added SHA-256 content-addressed CPU cache and checkpoint/resume.
- Added memory pressure/leak gate, CPU benchmark/profile, WASM SIMD detection with scalar fallback.
- Added incremental impact planner and CPU scene budget that preserves near-player quality/gameplay.
- Added CPU toolchain verification and explicit non-equivalence rule for 3DGS CPU fallback.
- Added service-role-only Supabase CPU profile/cache evidence tables.
- Desktop AI instructions now require fix->regression-test->rerun loop until all locally fixable errors are gone.
