(() => {'use strict';const G=globalThis;if(G.WorldProceduralProfiler)return;function create({targetFps=60}={}){let cpu=16.7,gpu=null,level=1,last=performance.now(),stable=0;const budget=1000/targetFps;return{begin(){last=performance.now()},end(){const dt=performance.now()-last;cpu=cpu*.9+dt*.1;if(cpu>budget*1.12){level=Math.max(.35,level-.06);stable=0}else if(cpu<budget*.72){stable++;if(stable>40){level=Math.min(1,level+.025);stable=0}}return level},setGpuMs(v){if(Number.isFinite(v))gpu=gpu==null?v:gpu*.85+v*.15},get(){return{cpuMs:cpu,gpuMs:gpu,budgetMs:budget,budgetScale:level}},passEnabled(cost=1){const pressure=Math.max(cpu,gpu||0)/budget;return pressure*cost<1.28||level>.82}}}G.WorldProceduralProfiler={version:'3.0.0',create};})();