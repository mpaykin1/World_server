# IMPROVE WORLD CORE LOOP + GRAPHICS INTEGRATION V1

This is the consolidation patch for the next product foundation. It does **not** replace the homepage yet. It creates the stable graphics/capability layer the future homepage will call.

Core contract: `answer -> derived traits -> world delta -> immediate visible mutation -> progressive refiners`.

Start with `00_START_HERE_DESKTOP_AI.md`. `install.cjs` without `--apply` is safe audit-only.
