# WORLD_SERVER PWA SELF-IMPROVING V4 MAX

## Architecture
`PWA -> device/runtime telemetry -> adaptive quality -> shader/stutter control -> predictive streaming -> source-preserving asset delivery -> Supabase evidence -> learned quality recommendation -> quality planner/autofix/AI sandbox -> convergence -> immutable Vercel preview -> Chromium/WebKit -> production -> real-iOS evidence -> rollback/revert`.

## V4 systems
- iPhone/desktop installable PWA, offline shell and versioned Service Worker;
- adaptive DPR/LOD/quality with hysteresis and remembered stable profile;
- JS heap, WebGL context-loss, Event Timing, frame p95/p99, long-task and stutter telemetry;
- shader compile/prewarm and stutter pressure response;
- camera/velocity-aware streaming hints and deterministic voxel predictive-center integration;
- runtime asset manifest + derived-asset map + bounded idle warming;
- optional free CPU KTX2/Basis + Meshopt derivative toolchain; source masters preserved;
- server-side learned per-app/per-capability quality recommendation from recent production evidence;
- rig discovery + semantic animation validator for feet, attacks, weapons and shields;
- mobile WebKit preview test plus separate real-iOS/standalone evidence gate;
- quiet self-improvement, convergence, canary, rollback and code-revert protections.

## Non-negotiable quality policy
Performance work must not delete or simplify working graphics. Prefer adaptive render scale, LOD, culling, predictive streaming, batching/instancing, cache, compression derivatives and shader prewarm. Any visual derivative must be regression-tested.

`BLOCKED_UNRESOLVED` is never completion. Continue root-cause repair and verification until `CLEAN`.
