#!/usr/bin/env node
'use strict';
const fs=require('fs'),path=require('path'),cp=require('child_process');const ROOT=process.cwd(),dir=path.join(ROOT,'.quality-artifacts/semantic-texture-smoke'),report=path.join(ROOT,'SEMANTIC_TEXTURE_BAKER_REPORT.json');fs.rmSync(dir,{recursive:true,force:true});fs.mkdirSync(dir,{recursive:true});
const py=process.env.PYTHON||process.env.PYTHON_BIN||(process.platform==='win32'?'python':'python3'),r=cp.spawnSync(py,[path.join(ROOT,'services/semantic-texture-baker/bake.py'),'--smoke','--semantic','stone','--size','160','--out',dir],{cwd:ROOT,encoding:'utf8',timeout:60000}),required=['albedo.png','normal.png','roughness.png','ao.png','height.png','manifest.json'],missing=required.filter(x=>!fs.existsSync(path.join(dir,x))),pass=r.status===0&&!missing.length,o={schemaVersion:'5.0.0',generatedAt:new Date().toISOString(),status:pass?'PASS':'FAIL',cpuOnly:true,missing,stdout:String(r.stdout||'').slice(-1000),stderr:String(r.stderr||'').slice(-1000)};fs.writeFileSync(report,JSON.stringify(o,null,2)+'\n');console.log(`[TEXTURE_BAKER_V5] ${o.status}`);if(!pass)process.exitCode=59;
