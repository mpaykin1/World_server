{
  "schemaVersion": "1.0.0",
  "provider": "cloudflare",
  "service": "world-server",
  "configuration": "wrangler.jsonc",
  "canonicalOriginEnvironmentVariable": "WORLD_SERVER_CANONICAL_ORIGIN",
  "previewOriginEnvironmentVariable": "CLOUDFLARE_DEPLOYMENT_URL",
  "deployedRevisionEnvironmentVariable": "WORLD_SERVER_DEPLOYED_SHA",
  "proofEndpoint": "/api/config",
  "runtimeHeader": {
    "name": "x-world-server-config-runtime",
    "value": "cloudflare-native"
  },
  "revisionHeader": "x-world-server-deployed-revision",
  "rule": "Resolve the exact-revision deploy URL from Cloudflare deployment output or the configured stable origin; never guess an account hostname. The canonical PUBLIC user-facing host is data/manual-delivery-policy.json (https://world-server.netlify.app); a Netlify deploy-preview alias is not canonical evidence, but a fresh-verified stable production Netlify URL may satisfy final user-facing link delivery."
}
