#!/usr/bin/env node
'use strict';const fs=require('node:fs');const {runCpuBenchmark}=require('../lib/quality/cpu-benchmark-v11');const {cpuSnapshot,deriveConcurrency}=require('../lib/quality/adaptive-cpu-scheduler-v11');const {detectWasmSimd}=require('../lib/quality/wasm-simd-capability-v11');
const snap=cpuSnapshot(),capacity=deriveConcurrency(snap,{reserveCores:1,maxWorkers:Number(process.env.QUALITY_CPU_MAX_WORKERS||8)}),bench=runCpuBenchmark({rounds:Number(process.env.QUALITY_CPU_BENCH_ROUNDS||256)});const out={generatedAt:new Date().toISOString(),snapshot:snap,capacity,benchmark:bench,wasm:detectWasmSimd(),verified:true};fs.mkdirSync('data/quality-autopilot',{recursive:true});fs.writeFileSync('data/quality-autopilot/cpu-profile-v11.json',JSON.stringify(out,null,2)+'\n');console.log(JSON.stringify(out,null,2));
