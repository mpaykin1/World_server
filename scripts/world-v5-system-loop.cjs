#!/usr/bin/env node
'use strict';const fs=require('fs'),path=require('path'),cp=require('child_process');const ROOT=path.resolve(__dirname,'..');
function run(script,args=[]){const f=path.join(ROOT,script);if(!fs.existsSync(f))return{script,skipped:true,ok:false};const r=cp.spawnSync(process.execPath,[f,...args],{cwd:ROOT,encoding:'utf8',env:process.env});if(r.stdout)process.stdout.write(r.stdout);if(r.stderr)process.stderr.write(r.stderr);return{script,ok:r.status===0,status:r.status}}
const steps=[run('scripts/world-google-deployment-learning-loop.cjs'),run('scripts/world-game-design-feedback-bridge.cjs'),run('scripts/world-game-design-to-tests.cjs'),run('scripts/world-feedback-development-bridge.cjs'),run('scripts/world-function-delivery-gate.cjs'),run('scripts/world-google-canary-plan.cjs')];
const report={schemaVersion:'5.0.0',system:'WORLD_V5_MUTUAL_REINFORCEMENT_LOOP',generatedAt:new Date().toISOString(),ok:steps.filter(x=>!x.skipped).every(x=>x.ok),steps,flow:['Google real-use evidence + user feedback','Game Design Spec delta','reuse existing system or define minimal function request','capability/security audit','derived acceptance + regression/replay tests','existing sandbox slot','cross-runtime/multiplayer/i18n/performance gates','Navigator immutable revision canary','last-green/rollback','new evidence returns to loop'],automaticProductionMutation:false};fs.writeFileSync(path.join(ROOT,'WORLD_V5_SYSTEM_LOOP_REPORT.json'),JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report,null,2));if(!report.ok)process.exitCode=2;
