#!/usr/bin/env node
'use strict';const fs=require('fs'),path=require('path');const ROOT=path.resolve(__dirname,'..'),g=JSON.parse(fs.readFileSync(path.join(ROOT,'google-ai-studio/v6-system-graph.json'),'utf8')),adj=new Map(g.nodes.map(n=>[n,[]]));for(const [a,b] of g.edges){if(adj.has(a))adj.get(a).push(b)}function reach(a){const s=new Set([a]),q=[a];while(q.length){for(const b of adj.get(q.shift())||[])if(!s.has(b)){s.add(b);q.push(b)}}return s}const anchors=['game-design-spec','capability-planner','function-registry','sandbox','tests-replay','release-promotion','google-cloud-evidence','feedback'];const problems=[];for(const a of ['navigator-dialogue','feedback','google-runtime','cost-quality']){const r=reach(a);for(const x of anchors)if(!r.has(x))problems.push(`${a} cannot reach ${x}`)}const report={schemaVersion:'6.0.0',generatedAt:new Date().toISOString(),pass:problems.length===0,nodes:g.nodes.length,edges:g.edges.length,problems};fs.writeFileSync(path.join(ROOT,'WORLD_V6_CONNECTIVITY_STATUS.json'),JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report,null,2));if(!report.pass)process.exitCode=2;
