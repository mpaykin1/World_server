'use strict';const fs=require('fs'),path=require('path');const S=require('../lib/world-semantic-feedback');const ROOT=path.resolve(__dirname,'..');let pass=true;const checks=[];function c(name,ok){checks.push({name,pass:Boolean(ok)});if(!ok)pass=false}const a=S.localEmbedding('camera joystick is broken'),b=S.localEmbedding('the camera joystick does not work'),z=S.localEmbedding('beautiful soundtrack and music');c('embedding-dimension',a.length===768);c('semantic-related-closer-than-unrelated',S.cosine(a,b)>S.cosine(a,z));c('gemini-embedding-2-contract',/gemini-embedding-2/.test(fs.readFileSync(path.join(ROOT,'lib/world-semantic-feedback.js'),'utf8')));c('pgvector-migration',/extensions\.vector\(768\)/.test(fs.readFileSync(path.join(ROOT,'supabase/migration_templates/world_community_v4.sql'),'utf8')));c('evidence-chain-installed',fs.existsSync(path.join(ROOT,'scripts/world-feedback-evidence-chain.cjs')));console.log(JSON.stringify({ok:pass,checks},null,2));if(!pass)process.exitCode=2;
