#!/usr/bin/env node
'use strict';const fs=require('fs'),path=require('path');const ROOT=path.resolve(__dirname,'..');const c=JSON.parse(fs.readFileSync(path.join(ROOT,'google-ai-studio/voice-scale-contract-v6.json'),'utf8')),s=fs.readFileSync(path.join(ROOT,'lib/api-handlers/livekit-token.js'),'utf8'),r=require('../shared/multiplayer/world-voice-router');const pass=r.chooseVoiceTransport({participants:4,sfuAvailable:true}).mode==='mesh'&&r.chooseVoiceTransport({participants:20,sfuAvailable:true}).mode==='livekit-sfu'&&r.chooseVoiceTransport({participants:20,sfuAvailable:false}).mode==='text-fallback'&&/requireUser/.test(s)&&/livekit-server-sdk/.test(s)&&!/apiSecretExposed:true/.test(s);const report={schemaVersion:'6.0.0',generatedAt:new Date().toISOString(),pass,contract:c,liveConfigured:Boolean(process.env.LIVEKIT_URL&&process.env.LIVEKIT_API_KEY&&process.env.LIVEKIT_API_SECRET)};fs.writeFileSync(path.join(ROOT,'WORLD_VOICE_SCALE_STATUS.json'),JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report,null,2));if(!pass)process.exitCode=2;
