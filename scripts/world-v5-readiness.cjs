#!/usr/bin/env node
'use strict';const fs=require('fs'),path=require('path');const ROOT=path.resolve(__dirname,'..');
function exists(p){return fs.existsSync(path.join(ROOT,p))}function boolEnv(k){return process.env[k]==='1'}
const staticChecks={googleLearning:exists('scripts/world-google-deployment-learning-loop.cjs')&&exists('google-ai-studio/google-learning-contract.json'),structuredCloudLogs:exists('google-ai-studio/cloudrun-entry.cjs'),functionRegistry:exists('lib/world-function-registry.js')&&exists('scripts/world-function-delivery-gate.cjs'),functionSchema:exists('supabase/migration_templates/world_functions_v5.sql'),gameDesign:exists('game-design/IMPROVE_WORLD_GAME_DESIGN_BASELINE.json')&&exists('scripts/world-game-design-gate.cjs'),gameDesignSchema:exists('supabase/migration_templates/world_game_design_v5.sql'),mutualLoop:exists('scripts/world-v5-system-loop.cjs'),v4:exists('scripts/world-v4-readiness.cjs')};
const liveChecks={googleRuntimeSignals:boolEnv('WORLD_GOOGLE_LEARNING_LIVE_VERIFIED'),functionSandboxInstall:boolEnv('WORLD_FUNCTION_DELIVERY_LIVE_VERIFIED'),gameDesignRoundTrip:boolEnv('WORLD_GAME_DESIGN_LIVE_VERIFIED'),navigatorCanaryRollback:boolEnv('WORLD_GOOGLE_CANARY_LIVE_VERIFIED'),v4Community:boolEnv('WORLD_V4_LIVE_VERIFIED')};
const pct=o=>Math.round(100*Object.values(o).filter(Boolean).length/Object.keys(o).length),staticPercent=pct(staticChecks),livePercent=pct(liveChecks),overallPercent=Math.round(staticPercent*.55+livePercent*.45);
const report={schemaVersion:'5.0.0',system:'WORLD_V5_READINESS',generatedAt:new Date().toISOString(),staticPercent,livePercent,overallPercent,staticChecks,liveChecks,meaning:{staticPercent:'implemented/verifiable in repository',livePercent:'requires real Google/Supabase/users/revisions evidence',overallPercent:'weighted readiness; static PASS can never become live PASS'},productionReady:overallPercent===100&&livePercent===100};fs.writeFileSync(path.join(ROOT,'WORLD_V5_READINESS.json'),JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report,null,2));
