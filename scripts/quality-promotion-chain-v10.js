#!/usr/bin/env node
'use strict';const fs=require('node:fs');const path=require('node:path');const {appendPromotionRecord,verifyPromotionChain}=require('../lib/quality/promotion-chain-v10');const file=path.resolve(process.env.QUALITY_PROMOTION_CHAIN||'data/quality-autopilot/promotion-chain-v10.json');let rows=[];if(fs.existsSync(file))rows=JSON.parse(fs.readFileSync(file,'utf8'));if(process.argv.includes('--verify')){const r=verifyPromotionChain(rows,process.env.QUALITY_ATTESTATION_SECRET||'');console.log(JSON.stringify(r,null,2));if(!r.ok)process.exitCode=2;}else{const last=rows.at(-1)?.recordHash||null;const row=appendPromotionRecord(last,{commitSha:process.env.GITHUB_SHA||process.env.QUALITY_COMMIT_SHA||'',deploymentId:process.env.QUALITY_DEPLOYMENT_ID||'',evidenceHash:process.env.QUALITY_EVIDENCE_HASH||'',decision:process.env.QUALITY_PROMOTION_DECISION||'HOLD'},process.env.QUALITY_ATTESTATION_SECRET||'');rows.push(row);fs.mkdirSync(path.dirname(file),{recursive:true});fs.writeFileSync(file,JSON.stringify(rows,null,2)+'\n');console.log(JSON.stringify(row,null,2));}
