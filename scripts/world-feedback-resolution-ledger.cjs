#!/usr/bin/env node
'use strict';const fs=require('fs'),path=require('path');const ROOT=path.resolve(__dirname,'..');function R(f,d={}){try{return JSON.parse(fs.readFileSync(path.join(ROOT,f),'utf8'))}catch{return d}}const candidates=R('.world/google-ai-studio/feedback-development-candidates.json',{candidates:[]}).candidates||[],gdd=R('WORLD_GAME_DESIGN_FEEDBACK_CANDIDATES.json',{candidates:[]}).candidates||[],prov=R('WORLD_FUNCTION_PROVENANCE_STATUS.json',{records:[]}).records||[],release=R('RELEASE_PROMOTION_DECISION.json',{});const ledger={schemaVersion:'6.0.0',generatedAt:new Date().toISOString(),links:{feedbackCandidates:candidates.length,gddCandidates:gdd.length,functionProvenance:prov.length,releaseDecision:release.decision||null},requiredChain:['feedback/cluster','GDD change candidate','function or reused capability','commit SHA','regression/replay evidence','sandbox revision','production revision','post-release outcome verification'],automaticClosure:false,rule:'A feedback item is resolved only after post-release evidence shows the requested outcome improved; shipping code alone is not resolution.'};fs.writeFileSync(path.join(ROOT,'WORLD_FEEDBACK_RESOLUTION_LEDGER_REPORT.json'),JSON.stringify(ledger,null,2)+'\n');console.log(JSON.stringify(ledger,null,2));
