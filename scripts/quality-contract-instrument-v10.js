#!/usr/bin/env node
'use strict';const fs=require('node:fs');const path=require('node:path');const {instrumentHtml}=require('../lib/quality/quality-contract-instrumenter-v10');const root=path.resolve(process.argv[2]||process.cwd()),apps=path.join(root,'apps');let changed=0,held=0;if(fs.existsSync(apps))for(const name of fs.readdirSync(apps)){const p=path.join(apps,name,'index.html');if(!fs.existsSync(p))continue;const r=instrumentHtml(fs.readFileSync(p,'utf8'));if(r.changed){if(process.argv.includes('--apply'))fs.writeFileSync(p,r.html);changed++;}else if(r.reason==='body-end-missing')held++;}console.log(JSON.stringify({ok:held===0,status:held?'HOLD':'PASS',candidates:changed,applied:process.argv.includes('--apply')?changed:0,held},null,2));if(held)process.exitCode=2;
