#!/usr/bin/env node
'use strict';const fs=require('fs'),path=require('path');const g=require('../lib/world-game-design-spec');const ROOT=path.resolve(__dirname,'..'),spec=g.loadBaseline(ROOT),v=g.validateSpec(spec),a=g.deriveAcceptanceTests(spec);const raw=JSON.stringify(spec).toLowerCase(),links={eye:/eye/.test(raw),navigator:/navigator/.test(raw),multiplayer:/multiplayer/.test(raw),feedback:/feedback/.test(raw),google:/google/.test(raw),functions:/function/.test(raw),languages:/language|translated/.test(raw),authoritative:/authoritative/.test(raw)};const pass=v.ok&&Object.values(links).every(Boolean)&&a.tests.length>=8;const report={schemaVersion:'5.0.0',system:'WORLD_GAME_DESIGN_GATE',generatedAt:new Date().toISOString(),pass,validation:v,links,acceptanceTests:a.tests.length,specHash:g.hashSpec(spec)};fs.writeFileSync(path.join(ROOT,'WORLD_GAME_DESIGN_GATE.json'),JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report,null,2));if(!pass)process.exit(2);
