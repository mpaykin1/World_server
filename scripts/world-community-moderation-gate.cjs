'use strict';const fs=require('fs'),path=require('path');const ROOT=path.resolve(__dirname,'..');const {SpamWindow,normalizeReport}=require('../lib/world-community-moderation');const checks=[];let ok=true;const c=(n,v)=>{checks.push({name:n,pass:Boolean(v)});if(!v)ok=false};const s=new SpamWindow({limit:2,windowMs:10000,duplicateLimit:1});c('spam-first-allowed',s.check('u','hello').allowed);c('spam-duplicate-blocked',!s.check('u','hello').allowed);c('report-normalized',normalizeReport({targetUserId:'u',reason:'spam'}).reason==='spam');for(const f of ['lib/api-handlers/community-message.js','lib/api-handlers/community-report.js','lib/api-handlers/feature-vote.js','lib/api-handlers/feedback-roadmap.js'])c(f,fs.existsSync(path.join(ROOT,f)));const sql=fs.readFileSync(path.join(ROOT,'supabase/migration_templates/world_community_v4.sql'),'utf8');c('reports-rls',/world_community_reports[\s\S]*enable row level security/i.test(sql));c('chat-server-broadcast',/realtime\.send/i.test(sql));console.log(JSON.stringify({ok,checks},null,2));if(!ok)process.exitCode=2;
