#!/usr/bin/env node
'use strict';const fs=require('fs'),path=require('path');const ROOT=path.resolve(__dirname,'..');const req=['apps/function-admin/index.html','lib/api-handlers/function-admin.js','lib/api-handlers/function-catalog.js','scripts/world-function-provenance.cjs','scripts/world-google-rollback-controller.cjs'];const missing=req.filter(x=>!fs.existsSync(path.join(ROOT,x)));const ui=missing.length?'':fs.readFileSync(path.join(ROOT,'apps/function-admin/index.html'),'utf8');const pass=!missing.length&&/0,1,5,25,50,100/.test(ui)&&/rollback/i.test(ui)&&/evidence/i.test(ui);const report={schemaVersion:'6.0.0',generatedAt:new Date().toISOString(),pass,missing,hardRules:['admin auth','no code upload in browser','rollout is config only','Navigator needs evidence','rollback command is operator-approved']};fs.writeFileSync(path.join(ROOT,'WORLD_FUNCTION_ADMIN_GATE.json'),JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report,null,2));if(!pass)process.exitCode=2;
