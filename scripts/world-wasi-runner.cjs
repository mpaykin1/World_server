#!/usr/bin/env node
'use strict';const fs=require('fs'),path=require('path'),cp=require('child_process');const ROOT=path.resolve(__dirname,'..'),base=path.join(ROOT,'world-functions-wasm');const rel=String(process.argv[2]||'');if(!/^[a-z0-9._\/-]+\.wasm$/i.test(rel))throw new Error('relative .wasm path required');const file=path.resolve(base,rel);if(!file.startsWith(path.resolve(base)+path.sep)||!fs.existsSync(file))throw new Error('WASM artifact must exist under world-functions-wasm');const args=['run',file,'--',...process.argv.slice(3).map(x=>String(x).slice(0,2048))];const r=cp.spawnSync('wasmtime',args,{cwd:ROOT,encoding:'utf8',timeout:Number(process.env.WORLD_WASI_TIMEOUT_MS||5000),env:{PATH:process.env.PATH||''}});process.stdout.write(r.stdout||'');process.stderr.write(r.stderr||'');if(r.error)throw r.error;if(r.status!==0)process.exit(r.status||2);
