#!/usr/bin/env node
'use strict';const fs=require('node:fs');const path=require('node:path');const {buildSbom,evaluateSupplyChain}=require('../lib/quality/sbom-provenance-v10');const root=path.resolve(process.argv[2]||process.cwd());const sbom=buildSbom(root),gate=evaluateSupplyChain(sbom,{requireLock:process.argv.includes('--require-lock'),requirePinned:process.argv.includes('--require-pinned')});const out={generatedAt:new Date().toISOString(),sbom,gate};fs.mkdirSync(path.join(root,'data/quality-autopilot'),{recursive:true});fs.writeFileSync(path.join(root,'data/quality-autopilot/sbom-v10.json'),JSON.stringify(out,null,2)+'\n');console.log(JSON.stringify(out,null,2));if(!gate.ok&&process.argv.includes('--strict'))process.exitCode=2;
