'use strict';const fs=require('fs'),path=require('path');const root=path.resolve(__dirname,'..');const contract=require('../google-ai-studio/multiplayer-contract-v3.json');const bridge=fs.readFileSync(path.join(root,'shared/multiplayer/world-multiplayer-bridge.js'),'utf8');const errors=[];if(!contract.transport.presence.includes('locale'))errors.push('Presence locale missing');if(contract.transport.presence.includes('player_state'))errors.push('High-frequency player_state must not use Presence');for(const x of ['player_state','chat_message'])if(!contract.transport.broadcast.includes(x))errors.push(`Broadcast missing ${x}`);if(!/private:true/.test(bridge))errors.push('Private channel not enforced in browser bridge');if(!/seq/.test(bridge))errors.push('Sequence dedupe missing');if(!/\/api\/translate/.test(bridge))errors.push('Translation bridge missing');console.log(JSON.stringify({ok:!errors.length,contractVersion:contract.schemaVersion,legacyGodotComponents:contract.compatibility.legacyGodotComponents,errors},null,2));if(errors.length)process.exitCode=1;
