 'use strict';const test=require('node:test'),assert=require('node:assert/strict'),profile=require('../lib/api-handlers/procedural-quality-profile.js'),orch=require('../lib/api-handlers/procedural-quality-orchestrate.js'),sem=require('../shared/procedural-quality-semantics.js');test('cinematic V3 enables G-buffer and GI',()=>{const p=profile.buildProfile({webgpu:'1',memory:'8',cores:'8',dpr:'2'});assert.equal(p.tier,'cinematic');assert.equal(p.policy.webgpuGBuffer,true);assert.ok(p.settings.gi>.4)});test('server orchestration reduces expensive passes on low FPS',()=>{const a=orch.tune({capabilities:{webgpu:true,memory:8,cores:8,dpr:2},fps:31,scene:{semanticConfidence:.8,motionConfidence:.2,coverage:.5,hasBrightLight:true}});assert.ok(a.settings.renderScale<1);assert.ok(a.settings.volumetric<.5)});test('semantic classifier recognizes bright upper light',()=>{const a=sem.analyze([{type:'rect',x:45,y:5,w:10,h:10,style:'#ffffff',frame:1},{type:'rect',x:0,y:0,w:100,h:100,style:'#222222',frame:1}],100,100,[]);assert.ok(a.groups.some(g=>g.role==='light'))});
