"use strict";const test=require('node:test'),assert=require('node:assert/strict');const {filterWorldVfxForSubscriber}=require('../lib/world-vfx-interest');test('server interest policy culls distant low-priority VFX',()=>{const x=filterWorldVfxForSubscriber([{id:'near',position:[1,0,0]},{id:'far',position:[100,0,0]},{id:'important',position:[100,0,0],priority:90}],{position:[0,0,0],radius:10});assert.deepEqual(x.map(e=>e.id),['important','near']);});
