(function(global){'use strict';
function finite(n){return Number.isFinite(Number(n))}function validateStateEnvelope(msg,{last=null,maxClockSkewMs=15000,maxSpeed=50,maxTeleport=35,minDtMs=15}={}){if(!msg||!msg.userId)return{ok:false,reason:'missing-user'};const s=msg.state||{};for(const k of ['x','y','z'])if(!finite(s[k]))return{ok:false,reason:`invalid-${k}`};const seq=Number(msg.seq||0);if(last&&seq<=Number(last.seq||0))return{ok:false,reason:'stale-sequence'};const at=Number(msg.at||Date.now());if(Math.abs(Date.now()-at)>maxClockSkewMs)return{ok:false,reason:'clock-skew'};if(last){const dt=Math.max(minDtMs,at-Number(last.at||at))/1000,dist=Math.hypot(Number(s.x)-Number(last.state.x),Number(s.y)-Number(last.state.y),Number(s.z)-Number(last.state.z));if(dist>maxTeleport)return{ok:false,reason:'teleport-threshold',distance:dist};if(dist/dt>maxSpeed)return{ok:false,reason:'speed-threshold',speed:dist/dt}}return{ok:true}}global.WorldPlayerStateAuthority={validateStateEnvelope};
})(globalThis);
