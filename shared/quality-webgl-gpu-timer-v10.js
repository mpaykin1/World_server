(function(g){'use strict';function create(gl){if(!gl||!gl.getExtension)return{available:false};const ext=gl.getExtension('EXT_disjoint_timer_query_webgl2');if(!ext||!gl.createQuery)return{available:false};return{available:true,begin(){const q=gl.createQuery();gl.beginQuery(ext.TIME_ELAPSED_EXT,q);return q;},end(){gl.endQuery(ext.TIME_ELAPSED_EXT);},poll(q){const ready=gl.getQueryParameter(q,gl.QUERY_RESULT_AVAILABLE),disjoint=gl.getParameter(ext.GPU_DISJOINT_EXT);if(!ready||disjoint)return{ready:false};return{ready:true,gpuMs:Number(gl.getQueryParameter(q,gl.QUERY_RESULT))/1e6};}}}g.QualityGpuTimerV10={create};})(window);
