const EKEY='darkVoidScienceEvidence:v2';
const mean=a=>a.length?a.reduce((s,x)=>s+x,0)/a.length:0;
export class ScienceEvidenceRecorder{
 constructor(){try{this.rows=JSON.parse(localStorage.getItem(EKEY)||'[]')}catch{this.rows=[]}}
 #save(){if(this.rows.length>1000)this.rows=this.rows.slice(-1000);try{localStorage.setItem(EKEY,JSON.stringify(this.rows))}catch{}}
 add(e={}){this.rows.push({...e,ts:e.ts??Date.now()});this.#save();return e}
 recordH1(step,data={}){return this.add({h:'H1',step,compactness:data.compactness??data.compressionRatio??0,recipeBytes:data.recipeBytes??null,blocks:data.blocks??data.manifestedBlocks??0,hash:data.hash??null})}
 recordH2(step,data={}){return this.add({h:'H2',step,blocks:data.blocks??data.structureCount??0,types:data.types??0,complexity:data.complexity??null,novelty:data.novelty??null,entropyRate:Number.isFinite(data.entropyRate)?data.entropyRate:null,mutualInformation:Number.isFinite(data.mutualInformation)?data.mutualInformation:null,shuffledMutualInformation:Number.isFinite(data.shuffledMutualInformation)?data.shuffledMutualInformation:null,organizedLift:Number.isFinite(data.organizedLift)?data.organizedLift:null,runCompressibility:Number.isFinite(data.runCompressibility)?data.runCompressibility:null,topologyComponents:Number.isFinite(data.topologyComponents)?data.topologyComponents:null,meanDegree:Number.isFinite(data.meanDegree)?data.meanDegree:null})}
 recordH3Prediction(step,data={}){return this.add({h:'H3',phase:'prediction',step,beforeManifestation:data.beforeManifestation===true,expected:data.expected??0,predictionHash:data.predictionHash??null,modelFamily:data.modelFamily??null})}
 recordH3Outcome(step,data={}){return this.add({h:'H3',phase:'outcome',step,expected:data.expected??0,actual:data.actual??0,matched:data.matched??null,missing:data.missing??null,added:data.added??null,matchRate:Number.isFinite(data.matchRate)?data.matchRate:null,delta:data.delta??0,match:data.match??null})}
 summary(){const h1=this.rows.filter(x=>x.h==='H1'),h2=this.rows.filter(x=>x.h==='H2'),pred=this.rows.filter(x=>x.h==='H3'&&x.phase==='prediction'),h3=this.rows.filter(x=>x.h==='H3'&&(x.phase==='outcome'||x.phase==null));return{schemaVersion:2,H1:{n:h1.length,meanCompactness:mean(h1.map(x=>x.compactness||0))},H2:{n:h2.length,meanBlocks:mean(h2.map(x=>x.blocks||0)),meanTypes:mean(h2.map(x=>x.types||0)),meanEntropyRate:mean(h2.map(x=>Number.isFinite(x.entropyRate)?x.entropyRate:0)),meanMutualInformation:mean(h2.map(x=>Number.isFinite(x.mutualInformation)?x.mutualInformation:0)),meanShuffledMutualInformation:mean(h2.map(x=>Number.isFinite(x.shuffledMutualInformation)?x.shuffledMutualInformation:0)),meanOrganizedLift:mean(h2.map(x=>Number.isFinite(x.organizedLift)?x.organizedLift:0)),meanRunCompressibility:mean(h2.map(x=>Number.isFinite(x.runCompressibility)?x.runCompressibility:0)),meanTopologyComponents:mean(h2.map(x=>Number.isFinite(x.topologyComponents)?x.topologyComponents:0)),meanDegree:mean(h2.map(x=>Number.isFinite(x.meanDegree)?x.meanDegree:0))},H3:{n:h3.length,predictions:pred.length,preManifestation:pred.filter(x=>x.beforeManifestation).length,matchRate:h3.length?mean(h3.map(x=>Number.isFinite(x.matchRate)?x.matchRate:(x.match?1:0))):0,meanAbsDelta:mean(h3.map(x=>Math.abs(x.delta||0))),meanMissing:mean(h3.map(x=>x.missing||0)),meanAdded:mean(h3.map(x=>x.added||0))}}}
 export(){return JSON.stringify({schemaVersion:2,rows:this.rows,summary:this.summary()},null,2)}
 clear(){this.rows.length=0;this.#save()}
}
