import test from 'node:test';import assert from 'node:assert/strict';import {encodeVfxBatch,decodeVfxBatch,VfxReplicationChannel} from '../runtime/replication-channel.mjs';test('binary replication batches deterministic recipes',()=>{const events=[{id:'a',type:'pulse',position:[0,0,0],seed:1},{id:'b',type:'sparks',position:[1,0,0],seed:2}];assert.deepEqual(decodeVfxBatch(encodeVfxBatch(events)).map(x=>x.id),['a','b']);let sent=null,spawned=0,t=1000;const c=new VfxReplicationChannel({send:p=>sent=p,spawn:()=>spawned++,now:()=>t});events.forEach(e=>c.enqueue(e));c.flush(true);assert.ok(sent);c.receive(sent);assert.equal(spawned,2);});