import test from 'node:test';import assert from 'node:assert/strict';import {VfxSurfaceResponseMemory} from '../runtime/surface-response-memory.mjs';test('surface response decays and preserves effect history',()=>{const m=new VfxSurfaceResponseMemory({halfLifeSeconds:10});m.record({position:[0,0,0],type:'decal',semantic:'impact',params:{intensity:2}});const a=m.query([0,0,0]).heat;m.tick(10);const b=m.query([0,0,0]).heat;assert.ok(a>b&&b>0);});