import test from 'node:test';import assert from 'node:assert/strict';import {VfxPipelineManifestCache} from '../runtime/pipeline-manifest-cache.mjs';test('shader manifest invalidates only on source change',()=>{const m=new Map(),s={getItem:k=>m.get(k)||null,setItem:(k,v)=>m.set(k,v)};const c=new VfxPipelineManifestCache({storage:s});assert.equal(c.needsWarmup('a','x'),true);c.markWarm('a','x');assert.equal(c.needsWarmup('a','x'),false);assert.equal(c.needsWarmup('a','y'),true);});