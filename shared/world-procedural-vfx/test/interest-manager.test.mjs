import test from 'node:test';import assert from 'node:assert/strict';import {VfxInterestManager} from '../runtime/interest-manager.mjs';test('interest manager keeps high priority and culls far effects',()=>{const m=new VfxInterestManager({tier:'low',camera:{position:{x:0,y:0,z:0}}});assert.equal(m.accept({position:[100,0,0],priority:0}).allow,false);assert.equal(m.accept({position:[100,0,0],priority:80}).allow,true);});