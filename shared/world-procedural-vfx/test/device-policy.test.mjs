import test from 'node:test';import assert from 'node:assert/strict';import {VfxDevicePolicy,createPrivacySafeDeviceClass} from '../runtime/device-policy.mjs';test('device policy downgrades deterministically without UA fingerprinting',()=>{const dc=createPrivacySafeDeviceClass({capabilities:{webgl2:true,instancing:true,floatTexture:false},deviceMemory:4,cores:4,coarse:true});assert.match(dc,/w21/);const p=new VfxDevicePolicy({deviceClass:dc,policy:{classes:{[dc]:{particleScale:.5,ceilingTier:'medium'}}},baseTier:'high'});assert.equal(p.ceilingTier(),'medium');assert.equal(p.transform({type:'sparks',params:{particleCount:100,intensity:1}}).params.particleCount,50);});