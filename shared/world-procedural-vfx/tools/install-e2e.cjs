#!/usr/bin/env node
const fs=require('fs'),path=require('path');let repo=process.cwd();for(let i=2;i<process.argv.length;i++)if(process.argv[i]==='--repo')repo=path.resolve(process.argv[++i]);const src=path.join(repo,'shared/world-procedural-vfx/examples/world-procedural-vfx.playwright.spec.js'),dst=path.join(repo,'e2e/world-procedural-vfx.spec.js');if(!fs.existsSync(src))throw new Error('VFX e2e source missing');fs.mkdirSync(path.dirname(dst),{recursive:true});const content=fs.readFileSync(src,'utf8');if(fs.existsSync(dst)&&fs.readFileSync(dst,'utf8')===content){console.log('VFX e2e already installed');process.exit(0);}fs.writeFileSync(dst,content);console.log(`Installed ${path.relative(repo,dst)}`);
